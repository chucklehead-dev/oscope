An example execution:

[State 0]
{
  lateArrivalTokenBackstop::fastPath::abandoned: Set(),
  lateArrivalTokenBackstop::fastPath::buffer: Set(),
  lateArrivalTokenBackstop::fastPath::ckpt: 0,
  lateArrivalTokenBackstop::fastPath::claimAt: Map(1 -> 0, 2 -> 0),
  lateArrivalTokenBackstop::fastPath::claims: Map(1 -> 0, 2 -> 0),
  lateArrivalTokenBackstop::fastPath::clock: 0,
  lateArrivalTokenBackstop::fastPath::commitAt: Map(1 -> -1, 2 -> -1),
  lateArrivalTokenBackstop::fastPath::committed: [],
  lateArrivalTokenBackstop::fastPath::events: Set(),
  lateArrivalTokenBackstop::fastPath::exps:
    Map(
      1 -> { attempts: 0, phase: "idle" },
      2 -> { attempts: 0, phase: "idle" }
    ),
  lateArrivalTokenBackstop::fastPath::imp: { b: 0, phase: "idle" },
  lateArrivalTokenBackstop::fastPath::impChecks: 0,
  lateArrivalTokenBackstop::fastPath::impInserts: 0,
  lateArrivalTokenBackstop::fastPath::ledger: Set(),
  lateArrivalTokenBackstop::fastPath::markers: Set(),
  lateArrivalTokenBackstop::fastPath::net: Set(),
  lateArrivalTokenBackstop::fastPath::target:
    Map((1, 1) -> 0, (1, 2) -> 0, (2, 1) -> 0, (2, 2) -> 0),
  lateArrivalTokenBackstop::fastPath::window: [],
  lateArrivalTokenBackstop::fastPath::written: Set(),
  lateArrivalTokenBackstop::fastPath::writtenAt: Map(1 -> -1, 2 -> -1)
}

[State 1]
{
  lateArrivalTokenBackstop::fastPath::abandoned: Set(),
  lateArrivalTokenBackstop::fastPath::buffer: Set(),
  lateArrivalTokenBackstop::fastPath::ckpt: 0,
  lateArrivalTokenBackstop::fastPath::claimAt: Map(1 -> 0, 2 -> 0),
  lateArrivalTokenBackstop::fastPath::claims: Map(1 -> 0, 2 -> 0),
  lateArrivalTokenBackstop::fastPath::clock: 0,
  lateArrivalTokenBackstop::fastPath::commitAt: Map(1 -> -1, 2 -> -1),
  lateArrivalTokenBackstop::fastPath::committed: [],
  lateArrivalTokenBackstop::fastPath::events: Set(),
  lateArrivalTokenBackstop::fastPath::exps:
    Map(
      1 -> { attempts: 0, phase: "idle" },
      2 -> { attempts: 0, phase: "gaveup" }
    ),
  lateArrivalTokenBackstop::fastPath::imp: { b: 0, phase: "idle" },
  lateArrivalTokenBackstop::fastPath::impChecks: 0,
  lateArrivalTokenBackstop::fastPath::impInserts: 0,
  lateArrivalTokenBackstop::fastPath::ledger: Set(),
  lateArrivalTokenBackstop::fastPath::markers: Set(),
  lateArrivalTokenBackstop::fastPath::net: Set(),
  lateArrivalTokenBackstop::fastPath::target:
    Map((1, 1) -> 0, (1, 2) -> 0, (2, 1) -> 0, (2, 2) -> 0),
  lateArrivalTokenBackstop::fastPath::window: [],
  lateArrivalTokenBackstop::fastPath::written: Set(),
  lateArrivalTokenBackstop::fastPath::writtenAt: Map(1 -> -1, 2 -> -1)
}

[State 2]
{
  lateArrivalTokenBackstop::fastPath::abandoned: Set(),
  lateArrivalTokenBackstop::fastPath::buffer: Set(),
  lateArrivalTokenBackstop::fastPath::ckpt: 0,
  lateArrivalTokenBackstop::fastPath::claimAt: Map(1 -> 0, 2 -> 0),
  lateArrivalTokenBackstop::fastPath::claims: Map(1 -> 0, 2 -> 0),
  lateArrivalTokenBackstop::fastPath::clock: 1,
  lateArrivalTokenBackstop::fastPath::commitAt: Map(1 -> -1, 2 -> -1),
  lateArrivalTokenBackstop::fastPath::committed: [],
  lateArrivalTokenBackstop::fastPath::events: Set(),
  lateArrivalTokenBackstop::fastPath::exps:
    Map(
      1 -> { attempts: 0, phase: "idle" },
      2 -> { attempts: 0, phase: "gaveup" }
    ),
  lateArrivalTokenBackstop::fastPath::imp: { b: 0, phase: "idle" },
  lateArrivalTokenBackstop::fastPath::impChecks: 0,
  lateArrivalTokenBackstop::fastPath::impInserts: 0,
  lateArrivalTokenBackstop::fastPath::ledger: Set(),
  lateArrivalTokenBackstop::fastPath::markers: Set(),
  lateArrivalTokenBackstop::fastPath::net: Set(),
  lateArrivalTokenBackstop::fastPath::target:
    Map((1, 1) -> 0, (1, 2) -> 0, (2, 1) -> 0, (2, 2) -> 0),
  lateArrivalTokenBackstop::fastPath::window: [],
  lateArrivalTokenBackstop::fastPath::written: Set(),
  lateArrivalTokenBackstop::fastPath::writtenAt: Map(1 -> -1, 2 -> -1)
}

[State 3]
{
  lateArrivalTokenBackstop::fastPath::abandoned: Set(),
  lateArrivalTokenBackstop::fastPath::buffer: Set(),
  lateArrivalTokenBackstop::fastPath::ckpt: 0,
  lateArrivalTokenBackstop::fastPath::claimAt: Map(1 -> 0, 2 -> 0),
  lateArrivalTokenBackstop::fastPath::claims: Map(1 -> 0, 2 -> 0),
  lateArrivalTokenBackstop::fastPath::clock: 2,
  lateArrivalTokenBackstop::fastPath::commitAt: Map(1 -> -1, 2 -> -1),
  lateArrivalTokenBackstop::fastPath::committed: [],
  lateArrivalTokenBackstop::fastPath::events: Set(),
  lateArrivalTokenBackstop::fastPath::exps:
    Map(
      1 -> { attempts: 0, phase: "idle" },
      2 -> { attempts: 0, phase: "gaveup" }
    ),
  lateArrivalTokenBackstop::fastPath::imp: { b: 0, phase: "idle" },
  lateArrivalTokenBackstop::fastPath::impChecks: 0,
  lateArrivalTokenBackstop::fastPath::impInserts: 0,
  lateArrivalTokenBackstop::fastPath::ledger: Set(),
  lateArrivalTokenBackstop::fastPath::markers: Set(),
  lateArrivalTokenBackstop::fastPath::net: Set(),
  lateArrivalTokenBackstop::fastPath::target:
    Map((1, 1) -> 0, (1, 2) -> 0, (2, 1) -> 0, (2, 2) -> 0),
  lateArrivalTokenBackstop::fastPath::window: [],
  lateArrivalTokenBackstop::fastPath::written: Set(),
  lateArrivalTokenBackstop::fastPath::writtenAt: Map(1 -> -1, 2 -> -1)
}

[State 4]
{
  lateArrivalTokenBackstop::fastPath::abandoned: Set(),
  lateArrivalTokenBackstop::fastPath::buffer: Set(),
  lateArrivalTokenBackstop::fastPath::ckpt: 0,
  lateArrivalTokenBackstop::fastPath::claimAt: Map(1 -> 0, 2 -> 0),
  lateArrivalTokenBackstop::fastPath::claims: Map(1 -> 0, 2 -> 0),
  lateArrivalTokenBackstop::fastPath::clock: 2,
  lateArrivalTokenBackstop::fastPath::commitAt: Map(1 -> -1, 2 -> -1),
  lateArrivalTokenBackstop::fastPath::committed: [],
  lateArrivalTokenBackstop::fastPath::events: Set(),
  lateArrivalTokenBackstop::fastPath::exps:
    Map(
      1 -> { attempts: 0, phase: "idle" },
      2 -> { attempts: 0, phase: "gaveup" }
    ),
  lateArrivalTokenBackstop::fastPath::imp: { b: 0, phase: "idle" },
  lateArrivalTokenBackstop::fastPath::impChecks: 0,
  lateArrivalTokenBackstop::fastPath::impInserts: 0,
  lateArrivalTokenBackstop::fastPath::ledger: Set(),
  lateArrivalTokenBackstop::fastPath::markers: Set(),
  lateArrivalTokenBackstop::fastPath::net: Set(),
  lateArrivalTokenBackstop::fastPath::target:
    Map((1, 1) -> 0, (1, 2) -> 0, (2, 1) -> 0, (2, 2) -> 0),
  lateArrivalTokenBackstop::fastPath::window: [],
  lateArrivalTokenBackstop::fastPath::written: Set(1),
  lateArrivalTokenBackstop::fastPath::writtenAt: Map(1 -> 2, 2 -> -1)
}

[State 5]
{
  lateArrivalTokenBackstop::fastPath::abandoned: Set(),
  lateArrivalTokenBackstop::fastPath::buffer: Set(),
  lateArrivalTokenBackstop::fastPath::ckpt: 0,
  lateArrivalTokenBackstop::fastPath::claimAt: Map(1 -> 0, 2 -> 0),
  lateArrivalTokenBackstop::fastPath::claims: Map(1 -> 0, 2 -> 0),
  lateArrivalTokenBackstop::fastPath::clock: 3,
  lateArrivalTokenBackstop::fastPath::commitAt: Map(1 -> -1, 2 -> -1),
  lateArrivalTokenBackstop::fastPath::committed: [],
  lateArrivalTokenBackstop::fastPath::events: Set(),
  lateArrivalTokenBackstop::fastPath::exps:
    Map(
      1 -> { attempts: 0, phase: "idle" },
      2 -> { attempts: 0, phase: "gaveup" }
    ),
  lateArrivalTokenBackstop::fastPath::imp: { b: 0, phase: "idle" },
  lateArrivalTokenBackstop::fastPath::impChecks: 0,
  lateArrivalTokenBackstop::fastPath::impInserts: 0,
  lateArrivalTokenBackstop::fastPath::ledger: Set(),
  lateArrivalTokenBackstop::fastPath::markers: Set(),
  lateArrivalTokenBackstop::fastPath::net: Set(),
  lateArrivalTokenBackstop::fastPath::target:
    Map((1, 1) -> 0, (1, 2) -> 0, (2, 1) -> 0, (2, 2) -> 0),
  lateArrivalTokenBackstop::fastPath::window: [],
  lateArrivalTokenBackstop::fastPath::written: Set(1),
  lateArrivalTokenBackstop::fastPath::writtenAt: Map(1 -> 2, 2 -> -1)
}

[State 6]
{
  lateArrivalTokenBackstop::fastPath::abandoned: Set(),
  lateArrivalTokenBackstop::fastPath::buffer: Set(),
  lateArrivalTokenBackstop::fastPath::ckpt: 0,
  lateArrivalTokenBackstop::fastPath::claimAt: Map(1 -> 0, 2 -> 0),
  lateArrivalTokenBackstop::fastPath::claims: Map(1 -> 0, 2 -> 0),
  lateArrivalTokenBackstop::fastPath::clock: 3,
  lateArrivalTokenBackstop::fastPath::commitAt: Map(1 -> 3, 2 -> -1),
  lateArrivalTokenBackstop::fastPath::committed: [1],
  lateArrivalTokenBackstop::fastPath::events: Set(),
  lateArrivalTokenBackstop::fastPath::exps:
    Map(
      1 -> { attempts: 0, phase: "idle" },
      2 -> { attempts: 0, phase: "gaveup" }
    ),
  lateArrivalTokenBackstop::fastPath::imp: { b: 0, phase: "idle" },
  lateArrivalTokenBackstop::fastPath::impChecks: 0,
  lateArrivalTokenBackstop::fastPath::impInserts: 0,
  lateArrivalTokenBackstop::fastPath::ledger: Set(),
  lateArrivalTokenBackstop::fastPath::markers: Set(),
  lateArrivalTokenBackstop::fastPath::net: Set(),
  lateArrivalTokenBackstop::fastPath::target:
    Map((1, 1) -> 0, (1, 2) -> 0, (2, 1) -> 0, (2, 2) -> 0),
  lateArrivalTokenBackstop::fastPath::window: [],
  lateArrivalTokenBackstop::fastPath::written: Set(1),
  lateArrivalTokenBackstop::fastPath::writtenAt: Map(1 -> 2, 2 -> -1)
}

[State 7]
{
  lateArrivalTokenBackstop::fastPath::abandoned: Set(),
  lateArrivalTokenBackstop::fastPath::buffer: Set(),
  lateArrivalTokenBackstop::fastPath::ckpt: 0,
  lateArrivalTokenBackstop::fastPath::claimAt: Map(1 -> 0, 2 -> 0),
  lateArrivalTokenBackstop::fastPath::claims: Map(1 -> 0, 2 -> 0),
  lateArrivalTokenBackstop::fastPath::clock: 4,
  lateArrivalTokenBackstop::fastPath::commitAt: Map(1 -> 3, 2 -> -1),
  lateArrivalTokenBackstop::fastPath::committed: [1],
  lateArrivalTokenBackstop::fastPath::events: Set(),
  lateArrivalTokenBackstop::fastPath::exps:
    Map(
      1 -> { attempts: 0, phase: "idle" },
      2 -> { attempts: 0, phase: "gaveup" }
    ),
  lateArrivalTokenBackstop::fastPath::imp: { b: 0, phase: "idle" },
  lateArrivalTokenBackstop::fastPath::impChecks: 0,
  lateArrivalTokenBackstop::fastPath::impInserts: 0,
  lateArrivalTokenBackstop::fastPath::ledger: Set(),
  lateArrivalTokenBackstop::fastPath::markers: Set(),
  lateArrivalTokenBackstop::fastPath::net: Set(),
  lateArrivalTokenBackstop::fastPath::target:
    Map((1, 1) -> 0, (1, 2) -> 0, (2, 1) -> 0, (2, 2) -> 0),
  lateArrivalTokenBackstop::fastPath::window: [],
  lateArrivalTokenBackstop::fastPath::written: Set(1),
  lateArrivalTokenBackstop::fastPath::writtenAt: Map(1 -> 2, 2 -> -1)
}

[State 8]
{
  lateArrivalTokenBackstop::fastPath::abandoned: Set(),
  lateArrivalTokenBackstop::fastPath::buffer: Set(),
  lateArrivalTokenBackstop::fastPath::ckpt: 0,
  lateArrivalTokenBackstop::fastPath::claimAt: Map(1 -> 0, 2 -> 0),
  lateArrivalTokenBackstop::fastPath::claims: Map(1 -> 0, 2 -> 0),
  lateArrivalTokenBackstop::fastPath::clock: 4,
  lateArrivalTokenBackstop::fastPath::commitAt: Map(1 -> 3, 2 -> -1),
  lateArrivalTokenBackstop::fastPath::committed: [1],
  lateArrivalTokenBackstop::fastPath::events: Set(),
  lateArrivalTokenBackstop::fastPath::exps:
    Map(
      1 -> { attempts: 0, phase: "idle" },
      2 -> { attempts: 0, phase: "gaveup" }
    ),
  lateArrivalTokenBackstop::fastPath::imp: { b: 0, phase: "idle" },
  lateArrivalTokenBackstop::fastPath::impChecks: 0,
  lateArrivalTokenBackstop::fastPath::impInserts: 0,
  lateArrivalTokenBackstop::fastPath::ledger: Set(),
  lateArrivalTokenBackstop::fastPath::markers: Set(),
  lateArrivalTokenBackstop::fastPath::net: Set(),
  lateArrivalTokenBackstop::fastPath::target:
    Map((1, 1) -> 0, (1, 2) -> 0, (2, 1) -> 0, (2, 2) -> 0),
  lateArrivalTokenBackstop::fastPath::window: [],
  lateArrivalTokenBackstop::fastPath::written: Set(1, 2),
  lateArrivalTokenBackstop::fastPath::writtenAt: Map(1 -> 2, 2 -> 4)
}

[State 9]
{
  lateArrivalTokenBackstop::fastPath::abandoned: Set(),
  lateArrivalTokenBackstop::fastPath::buffer: Set(),
  lateArrivalTokenBackstop::fastPath::ckpt: 0,
  lateArrivalTokenBackstop::fastPath::claimAt: Map(1 -> 0, 2 -> 0),
  lateArrivalTokenBackstop::fastPath::claims: Map(1 -> 0, 2 -> 0),
  lateArrivalTokenBackstop::fastPath::clock: 5,
  lateArrivalTokenBackstop::fastPath::commitAt: Map(1 -> 3, 2 -> -1),
  lateArrivalTokenBackstop::fastPath::committed: [1],
  lateArrivalTokenBackstop::fastPath::events: Set(),
  lateArrivalTokenBackstop::fastPath::exps:
    Map(
      1 -> { attempts: 0, phase: "idle" },
      2 -> { attempts: 0, phase: "gaveup" }
    ),
  lateArrivalTokenBackstop::fastPath::imp: { b: 0, phase: "idle" },
  lateArrivalTokenBackstop::fastPath::impChecks: 0,
  lateArrivalTokenBackstop::fastPath::impInserts: 0,
  lateArrivalTokenBackstop::fastPath::ledger: Set(),
  lateArrivalTokenBackstop::fastPath::markers: Set(),
  lateArrivalTokenBackstop::fastPath::net: Set(),
  lateArrivalTokenBackstop::fastPath::target:
    Map((1, 1) -> 0, (1, 2) -> 0, (2, 1) -> 0, (2, 2) -> 0),
  lateArrivalTokenBackstop::fastPath::window: [],
  lateArrivalTokenBackstop::fastPath::written: Set(1, 2),
  lateArrivalTokenBackstop::fastPath::writtenAt: Map(1 -> 2, 2 -> 4)
}

[State 10]
{
  lateArrivalTokenBackstop::fastPath::abandoned: Set(),
  lateArrivalTokenBackstop::fastPath::buffer: Set(),
  lateArrivalTokenBackstop::fastPath::ckpt: 0,
  lateArrivalTokenBackstop::fastPath::claimAt: Map(1 -> 0, 2 -> 0),
  lateArrivalTokenBackstop::fastPath::claims: Map(1 -> 0, 2 -> 0),
  lateArrivalTokenBackstop::fastPath::clock: 5,
  lateArrivalTokenBackstop::fastPath::commitAt: Map(1 -> 3, 2 -> 5),
  lateArrivalTokenBackstop::fastPath::committed: [1, 2],
  lateArrivalTokenBackstop::fastPath::events: Set(),
  lateArrivalTokenBackstop::fastPath::exps:
    Map(
      1 -> { attempts: 0, phase: "idle" },
      2 -> { attempts: 0, phase: "gaveup" }
    ),
  lateArrivalTokenBackstop::fastPath::imp: { b: 0, phase: "idle" },
  lateArrivalTokenBackstop::fastPath::impChecks: 0,
  lateArrivalTokenBackstop::fastPath::impInserts: 0,
  lateArrivalTokenBackstop::fastPath::ledger: Set(),
  lateArrivalTokenBackstop::fastPath::markers: Set(),
  lateArrivalTokenBackstop::fastPath::net: Set(),
  lateArrivalTokenBackstop::fastPath::target:
    Map((1, 1) -> 0, (1, 2) -> 0, (2, 1) -> 0, (2, 2) -> 0),
  lateArrivalTokenBackstop::fastPath::window: [],
  lateArrivalTokenBackstop::fastPath::written: Set(1, 2),
  lateArrivalTokenBackstop::fastPath::writtenAt: Map(1 -> 2, 2 -> 4)
}

[State 11]
{
  lateArrivalTokenBackstop::fastPath::abandoned: Set(),
  lateArrivalTokenBackstop::fastPath::buffer: Set(),
  lateArrivalTokenBackstop::fastPath::ckpt: 0,
  lateArrivalTokenBackstop::fastPath::claimAt: Map(1 -> 0, 2 -> 0),
  lateArrivalTokenBackstop::fastPath::claims: Map(1 -> 0, 2 -> 0),
  lateArrivalTokenBackstop::fastPath::clock: 6,
  lateArrivalTokenBackstop::fastPath::commitAt: Map(1 -> 3, 2 -> 5),
  lateArrivalTokenBackstop::fastPath::committed: [1, 2],
  lateArrivalTokenBackstop::fastPath::events: Set(),
  lateArrivalTokenBackstop::fastPath::exps:
    Map(
      1 -> { attempts: 0, phase: "idle" },
      2 -> { attempts: 0, phase: "gaveup" }
    ),
  lateArrivalTokenBackstop::fastPath::imp: { b: 0, phase: "idle" },
  lateArrivalTokenBackstop::fastPath::impChecks: 0,
  lateArrivalTokenBackstop::fastPath::impInserts: 0,
  lateArrivalTokenBackstop::fastPath::ledger: Set(),
  lateArrivalTokenBackstop::fastPath::markers: Set(),
  lateArrivalTokenBackstop::fastPath::net: Set(),
  lateArrivalTokenBackstop::fastPath::target:
    Map((1, 1) -> 0, (1, 2) -> 0, (2, 1) -> 0, (2, 2) -> 0),
  lateArrivalTokenBackstop::fastPath::window: [],
  lateArrivalTokenBackstop::fastPath::written: Set(1, 2),
  lateArrivalTokenBackstop::fastPath::writtenAt: Map(1 -> 2, 2 -> 4)
}

[State 12]
{
  lateArrivalTokenBackstop::fastPath::abandoned: Set(),
  lateArrivalTokenBackstop::fastPath::buffer: Set(),
  lateArrivalTokenBackstop::fastPath::ckpt: 0,
  lateArrivalTokenBackstop::fastPath::claimAt: Map(1 -> 0, 2 -> 0),
  lateArrivalTokenBackstop::fastPath::claims: Map(1 -> 0, 2 -> 0),
  lateArrivalTokenBackstop::fastPath::clock: 7,
  lateArrivalTokenBackstop::fastPath::commitAt: Map(1 -> 3, 2 -> 5),
  lateArrivalTokenBackstop::fastPath::committed: [1, 2],
  lateArrivalTokenBackstop::fastPath::events: Set(),
  lateArrivalTokenBackstop::fastPath::exps:
    Map(
      1 -> { attempts: 0, phase: "idle" },
      2 -> { attempts: 0, phase: "gaveup" }
    ),
  lateArrivalTokenBackstop::fastPath::imp: { b: 0, phase: "idle" },
  lateArrivalTokenBackstop::fastPath::impChecks: 0,
  lateArrivalTokenBackstop::fastPath::impInserts: 0,
  lateArrivalTokenBackstop::fastPath::ledger: Set(),
  lateArrivalTokenBackstop::fastPath::markers: Set(),
  lateArrivalTokenBackstop::fastPath::net: Set(),
  lateArrivalTokenBackstop::fastPath::target:
    Map((1, 1) -> 0, (1, 2) -> 0, (2, 1) -> 0, (2, 2) -> 0),
  lateArrivalTokenBackstop::fastPath::window: [],
  lateArrivalTokenBackstop::fastPath::written: Set(1, 2),
  lateArrivalTokenBackstop::fastPath::writtenAt: Map(1 -> 2, 2 -> 4)
}

[State 13]
{
  lateArrivalTokenBackstop::fastPath::abandoned: Set(),
  lateArrivalTokenBackstop::fastPath::buffer: Set(),
  lateArrivalTokenBackstop::fastPath::ckpt: 0,
  lateArrivalTokenBackstop::fastPath::claimAt: Map(1 -> 0, 2 -> 0),
  lateArrivalTokenBackstop::fastPath::claims: Map(1 -> 0, 2 -> 0),
  lateArrivalTokenBackstop::fastPath::clock: 7,
  lateArrivalTokenBackstop::fastPath::commitAt: Map(1 -> 3, 2 -> 5),
  lateArrivalTokenBackstop::fastPath::committed: [1, 2],
  lateArrivalTokenBackstop::fastPath::events: Set("impPartial"),
  lateArrivalTokenBackstop::fastPath::exps:
    Map(
      1 -> { attempts: 0, phase: "idle" },
      2 -> { attempts: 0, phase: "gaveup" }
    ),
  lateArrivalTokenBackstop::fastPath::imp: { b: 0, phase: "idle" },
  lateArrivalTokenBackstop::fastPath::impChecks: 1,
  lateArrivalTokenBackstop::fastPath::impInserts: 1,
  lateArrivalTokenBackstop::fastPath::ledger: Set(),
  lateArrivalTokenBackstop::fastPath::markers: Set(),
  lateArrivalTokenBackstop::fastPath::net: Set(),
  lateArrivalTokenBackstop::fastPath::target:
    Map((1, 1) -> 0, (1, 2) -> 0, (2, 1) -> 0, (2, 2) -> 0),
  lateArrivalTokenBackstop::fastPath::window: [],
  lateArrivalTokenBackstop::fastPath::written: Set(1, 2),
  lateArrivalTokenBackstop::fastPath::writtenAt: Map(1 -> 2, 2 -> 4)
}

[State 14]
{
  lateArrivalTokenBackstop::fastPath::abandoned: Set(),
  lateArrivalTokenBackstop::fastPath::buffer: Set(),
  lateArrivalTokenBackstop::fastPath::ckpt: 0,
  lateArrivalTokenBackstop::fastPath::claimAt: Map(1 -> 0, 2 -> 0),
  lateArrivalTokenBackstop::fastPath::claims: Map(1 -> 0, 2 -> 0),
  lateArrivalTokenBackstop::fastPath::clock: 7,
  lateArrivalTokenBackstop::fastPath::commitAt: Map(1 -> 3, 2 -> 5),
  lateArrivalTokenBackstop::fastPath::committed: [1, 2],
  lateArrivalTokenBackstop::fastPath::events: Set("impPartial"),
  lateArrivalTokenBackstop::fastPath::exps:
    Map(
      1 -> { attempts: 0, phase: "idle" },
      2 -> { attempts: 0, phase: "gaveup" }
    ),
  lateArrivalTokenBackstop::fastPath::imp: { b: 0, phase: "idle" },
  lateArrivalTokenBackstop::fastPath::impChecks: 2,
  lateArrivalTokenBackstop::fastPath::impInserts: 2,
  lateArrivalTokenBackstop::fastPath::ledger: Set(),
  lateArrivalTokenBackstop::fastPath::markers: Set(),
  lateArrivalTokenBackstop::fastPath::net: Set(),
  lateArrivalTokenBackstop::fastPath::target:
    Map((1, 1) -> 1, (1, 2) -> 0, (2, 1) -> 0, (2, 2) -> 0),
  lateArrivalTokenBackstop::fastPath::window: [(1, 1)],
  lateArrivalTokenBackstop::fastPath::written: Set(1, 2),
  lateArrivalTokenBackstop::fastPath::writtenAt: Map(1 -> 2, 2 -> 4)
}

[State 15]
{
  lateArrivalTokenBackstop::fastPath::abandoned: Set(),
  lateArrivalTokenBackstop::fastPath::buffer: Set(),
  lateArrivalTokenBackstop::fastPath::ckpt: 0,
  lateArrivalTokenBackstop::fastPath::claimAt: Map(1 -> 0, 2 -> 0),
  lateArrivalTokenBackstop::fastPath::claims: Map(1 -> 0, 2 -> 0),
  lateArrivalTokenBackstop::fastPath::clock: 7,
  lateArrivalTokenBackstop::fastPath::commitAt: Map(1 -> 3, 2 -> 5),
  lateArrivalTokenBackstop::fastPath::committed: [1, 2],
  lateArrivalTokenBackstop::fastPath::events: Set("impPartial"),
  lateArrivalTokenBackstop::fastPath::exps:
    Map(
      1 -> { attempts: 0, phase: "gaveup" },
      2 -> { attempts: 0, phase: "gaveup" }
    ),
  lateArrivalTokenBackstop::fastPath::imp: { b: 0, phase: "idle" },
  lateArrivalTokenBackstop::fastPath::impChecks: 2,
  lateArrivalTokenBackstop::fastPath::impInserts: 2,
  lateArrivalTokenBackstop::fastPath::ledger: Set(),
  lateArrivalTokenBackstop::fastPath::markers: Set(),
  lateArrivalTokenBackstop::fastPath::net: Set(),
  lateArrivalTokenBackstop::fastPath::target:
    Map((1, 1) -> 1, (1, 2) -> 0, (2, 1) -> 0, (2, 2) -> 0),
  lateArrivalTokenBackstop::fastPath::window: [(1, 1)],
  lateArrivalTokenBackstop::fastPath::written: Set(1, 2),
  lateArrivalTokenBackstop::fastPath::writtenAt: Map(1 -> 2, 2 -> 4)
}

[State 16]
{
  lateArrivalTokenBackstop::fastPath::abandoned: Set(),
  lateArrivalTokenBackstop::fastPath::buffer: Set(),
  lateArrivalTokenBackstop::fastPath::ckpt: 0,
  lateArrivalTokenBackstop::fastPath::claimAt: Map(1 -> 0, 2 -> 0),
  lateArrivalTokenBackstop::fastPath::claims: Map(1 -> 0, 2 -> 0),
  lateArrivalTokenBackstop::fastPath::clock: 7,
  lateArrivalTokenBackstop::fastPath::commitAt: Map(1 -> 3, 2 -> 5),
  lateArrivalTokenBackstop::fastPath::committed: [1, 2],
  lateArrivalTokenBackstop::fastPath::events: Set("impInserted", "impPartial"),
  lateArrivalTokenBackstop::fastPath::exps:
    Map(
      1 -> { attempts: 0, phase: "gaveup" },
      2 -> { attempts: 0, phase: "gaveup" }
    ),
  lateArrivalTokenBackstop::fastPath::imp: { b: 1, phase: "done" },
  lateArrivalTokenBackstop::fastPath::impChecks: 3,
  lateArrivalTokenBackstop::fastPath::impInserts: 3,
  lateArrivalTokenBackstop::fastPath::ledger: Set(),
  lateArrivalTokenBackstop::fastPath::markers: Set(),
  lateArrivalTokenBackstop::fastPath::net: Set(),
  lateArrivalTokenBackstop::fastPath::target:
    Map((1, 1) -> 1, (1, 2) -> 1, (2, 1) -> 0, (2, 2) -> 0),
  lateArrivalTokenBackstop::fastPath::window: [(1, 1), (1, 2)],
  lateArrivalTokenBackstop::fastPath::written: Set(1, 2),
  lateArrivalTokenBackstop::fastPath::writtenAt: Map(1 -> 2, 2 -> 4)
}

[State 17]
{
  lateArrivalTokenBackstop::fastPath::abandoned: Set(),
  lateArrivalTokenBackstop::fastPath::buffer: Set(),
  lateArrivalTokenBackstop::fastPath::ckpt: 0,
  lateArrivalTokenBackstop::fastPath::claimAt: Map(1 -> 0, 2 -> 0),
  lateArrivalTokenBackstop::fastPath::claims: Map(1 -> 0, 2 -> 0),
  lateArrivalTokenBackstop::fastPath::clock: 7,
  lateArrivalTokenBackstop::fastPath::commitAt: Map(1 -> 3, 2 -> 5),
  lateArrivalTokenBackstop::fastPath::committed: [1, 2],
  lateArrivalTokenBackstop::fastPath::events:
    Set("impCrash", "impInserted", "impPartial"),
  lateArrivalTokenBackstop::fastPath::exps:
    Map(
      1 -> { attempts: 0, phase: "gaveup" },
      2 -> { attempts: 0, phase: "gaveup" }
    ),
  lateArrivalTokenBackstop::fastPath::imp: { b: 0, phase: "idle" },
  lateArrivalTokenBackstop::fastPath::impChecks: 3,
  lateArrivalTokenBackstop::fastPath::impInserts: 3,
  lateArrivalTokenBackstop::fastPath::ledger: Set(),
  lateArrivalTokenBackstop::fastPath::markers: Set(),
  lateArrivalTokenBackstop::fastPath::net: Set(),
  lateArrivalTokenBackstop::fastPath::target:
    Map((1, 1) -> 1, (1, 2) -> 1, (2, 1) -> 0, (2, 2) -> 0),
  lateArrivalTokenBackstop::fastPath::window: [(1, 1), (1, 2)],
  lateArrivalTokenBackstop::fastPath::written: Set(1, 2),
  lateArrivalTokenBackstop::fastPath::writtenAt: Map(1 -> 2, 2 -> 4)
}

[State 18]
{
  lateArrivalTokenBackstop::fastPath::abandoned: Set(),
  lateArrivalTokenBackstop::fastPath::buffer: Set(),
  lateArrivalTokenBackstop::fastPath::ckpt: 0,
  lateArrivalTokenBackstop::fastPath::claimAt: Map(1 -> 0, 2 -> 0),
  lateArrivalTokenBackstop::fastPath::claims: Map(1 -> 0, 2 -> 0),
  lateArrivalTokenBackstop::fastPath::clock: 7,
  lateArrivalTokenBackstop::fastPath::commitAt: Map(1 -> 3, 2 -> 5),
  lateArrivalTokenBackstop::fastPath::committed: [1, 2],
  lateArrivalTokenBackstop::fastPath::events:
    Set("impCrash", "impInserted", "impPartial", "impSkipped"),
  lateArrivalTokenBackstop::fastPath::exps:
    Map(
      1 -> { attempts: 0, phase: "gaveup" },
      2 -> { attempts: 0, phase: "gaveup" }
    ),
  lateArrivalTokenBackstop::fastPath::imp: { b: 1, phase: "done" },
  lateArrivalTokenBackstop::fastPath::impChecks: 4,
  lateArrivalTokenBackstop::fastPath::impInserts: 3,
  lateArrivalTokenBackstop::fastPath::ledger: Set(),
  lateArrivalTokenBackstop::fastPath::markers: Set(),
  lateArrivalTokenBackstop::fastPath::net: Set(),
  lateArrivalTokenBackstop::fastPath::target:
    Map((1, 1) -> 1, (1, 2) -> 1, (2, 1) -> 0, (2, 2) -> 0),
  lateArrivalTokenBackstop::fastPath::window: [(1, 1), (1, 2)],
  lateArrivalTokenBackstop::fastPath::written: Set(1, 2),
  lateArrivalTokenBackstop::fastPath::writtenAt: Map(1 -> 2, 2 -> 4)
}

[State 19]
{
  lateArrivalTokenBackstop::fastPath::abandoned: Set(),
  lateArrivalTokenBackstop::fastPath::buffer: Set(),
  lateArrivalTokenBackstop::fastPath::ckpt: 0,
  lateArrivalTokenBackstop::fastPath::claimAt: Map(1 -> 0, 2 -> 0),
  lateArrivalTokenBackstop::fastPath::claims: Map(1 -> 0, 2 -> 0),
  lateArrivalTokenBackstop::fastPath::clock: 7,
  lateArrivalTokenBackstop::fastPath::commitAt: Map(1 -> 3, 2 -> 5),
  lateArrivalTokenBackstop::fastPath::committed: [1, 2],
  lateArrivalTokenBackstop::fastPath::events:
    Set("impCrash", "impInserted", "impPartial", "impSkipped"),
  lateArrivalTokenBackstop::fastPath::exps:
    Map(
      1 -> { attempts: 0, phase: "gaveup" },
      2 -> { attempts: 0, phase: "gaveup" }
    ),
  lateArrivalTokenBackstop::fastPath::imp: { b: 0, phase: "idle" },
  lateArrivalTokenBackstop::fastPath::impChecks: 4,
  lateArrivalTokenBackstop::fastPath::impInserts: 3,
  lateArrivalTokenBackstop::fastPath::ledger: Set(),
  lateArrivalTokenBackstop::fastPath::markers: Set(),
  lateArrivalTokenBackstop::fastPath::net: Set(),
  lateArrivalTokenBackstop::fastPath::target:
    Map((1, 1) -> 1, (1, 2) -> 1, (2, 1) -> 0, (2, 2) -> 0),
  lateArrivalTokenBackstop::fastPath::window: [(1, 1), (1, 2)],
  lateArrivalTokenBackstop::fastPath::written: Set(1, 2),
  lateArrivalTokenBackstop::fastPath::writtenAt: Map(1 -> 2, 2 -> 4)
}

[State 20]
{
  lateArrivalTokenBackstop::fastPath::abandoned: Set(),
  lateArrivalTokenBackstop::fastPath::buffer: Set(),
  lateArrivalTokenBackstop::fastPath::ckpt: 0,
  lateArrivalTokenBackstop::fastPath::claimAt: Map(1 -> 0, 2 -> 0),
  lateArrivalTokenBackstop::fastPath::claims: Map(1 -> 0, 2 -> 0),
  lateArrivalTokenBackstop::fastPath::clock: 7,
  lateArrivalTokenBackstop::fastPath::commitAt: Map(1 -> 3, 2 -> 5),
  lateArrivalTokenBackstop::fastPath::committed: [1, 2],
  lateArrivalTokenBackstop::fastPath::events:
    Set("impCrash", "impInserted", "impPartial", "impSkipped"),
  lateArrivalTokenBackstop::fastPath::exps:
    Map(
      1 -> { attempts: 0, phase: "gaveup" },
      2 -> { attempts: 0, phase: "gaveup" }
    ),
  lateArrivalTokenBackstop::fastPath::imp: { b: 1, phase: "done" },
  lateArrivalTokenBackstop::fastPath::impChecks: 5,
  lateArrivalTokenBackstop::fastPath::impInserts: 3,
  lateArrivalTokenBackstop::fastPath::ledger: Set(),
  lateArrivalTokenBackstop::fastPath::markers: Set(),
  lateArrivalTokenBackstop::fastPath::net: Set(),
  lateArrivalTokenBackstop::fastPath::target:
    Map((1, 1) -> 1, (1, 2) -> 1, (2, 1) -> 0, (2, 2) -> 0),
  lateArrivalTokenBackstop::fastPath::window: [(1, 1), (1, 2)],
  lateArrivalTokenBackstop::fastPath::written: Set(1, 2),
  lateArrivalTokenBackstop::fastPath::writtenAt: Map(1 -> 2, 2 -> 4)
}

[State 21]
{
  lateArrivalTokenBackstop::fastPath::abandoned: Set(),
  lateArrivalTokenBackstop::fastPath::buffer: Set(),
  lateArrivalTokenBackstop::fastPath::ckpt: 0,
  lateArrivalTokenBackstop::fastPath::claimAt: Map(1 -> 0, 2 -> 0),
  lateArrivalTokenBackstop::fastPath::claims: Map(1 -> 0, 2 -> 0),
  lateArrivalTokenBackstop::fastPath::clock: 7,
  lateArrivalTokenBackstop::fastPath::commitAt: Map(1 -> 3, 2 -> 5),
  lateArrivalTokenBackstop::fastPath::committed: [1, 2],
  lateArrivalTokenBackstop::fastPath::events:
    Set("impCrash", "impInserted", "impPartial", "impSkipped"),
  lateArrivalTokenBackstop::fastPath::exps:
    Map(
      1 -> { attempts: 0, phase: "gaveup" },
      2 -> { attempts: 0, phase: "gaveup" }
    ),
  lateArrivalTokenBackstop::fastPath::imp: { b: 0, phase: "idle" },
  lateArrivalTokenBackstop::fastPath::impChecks: 5,
  lateArrivalTokenBackstop::fastPath::impInserts: 3,
  lateArrivalTokenBackstop::fastPath::ledger: Set(),
  lateArrivalTokenBackstop::fastPath::markers: Set(),
  lateArrivalTokenBackstop::fastPath::net: Set(),
  lateArrivalTokenBackstop::fastPath::target:
    Map((1, 1) -> 1, (1, 2) -> 1, (2, 1) -> 0, (2, 2) -> 0),
  lateArrivalTokenBackstop::fastPath::window: [(1, 1), (1, 2)],
  lateArrivalTokenBackstop::fastPath::written: Set(1, 2),
  lateArrivalTokenBackstop::fastPath::writtenAt: Map(1 -> 2, 2 -> 4)
}

[State 22]
{
  lateArrivalTokenBackstop::fastPath::abandoned: Set(),
  lateArrivalTokenBackstop::fastPath::buffer: Set(),
  lateArrivalTokenBackstop::fastPath::ckpt: 0,
  lateArrivalTokenBackstop::fastPath::claimAt: Map(1 -> 0, 2 -> 0),
  lateArrivalTokenBackstop::fastPath::claims: Map(1 -> 0, 2 -> 0),
  lateArrivalTokenBackstop::fastPath::clock: 7,
  lateArrivalTokenBackstop::fastPath::commitAt: Map(1 -> 3, 2 -> 5),
  lateArrivalTokenBackstop::fastPath::committed: [1, 2],
  lateArrivalTokenBackstop::fastPath::events:
    Set("impCrash", "impInserted", "impPartial", "impSkipped"),
  lateArrivalTokenBackstop::fastPath::exps:
    Map(
      1 -> { attempts: 0, phase: "gaveup" },
      2 -> { attempts: 0, phase: "gaveup" }
    ),
  lateArrivalTokenBackstop::fastPath::imp: { b: 1, phase: "done" },
  lateArrivalTokenBackstop::fastPath::impChecks: 6,
  lateArrivalTokenBackstop::fastPath::impInserts: 3,
  lateArrivalTokenBackstop::fastPath::ledger: Set(),
  lateArrivalTokenBackstop::fastPath::markers: Set(),
  lateArrivalTokenBackstop::fastPath::net: Set(),
  lateArrivalTokenBackstop::fastPath::target:
    Map((1, 1) -> 1, (1, 2) -> 1, (2, 1) -> 0, (2, 2) -> 0),
  lateArrivalTokenBackstop::fastPath::window: [(1, 1), (1, 2)],
  lateArrivalTokenBackstop::fastPath::written: Set(1, 2),
  lateArrivalTokenBackstop::fastPath::writtenAt: Map(1 -> 2, 2 -> 4)
}

[State 23]
{
  lateArrivalTokenBackstop::fastPath::abandoned: Set(),
  lateArrivalTokenBackstop::fastPath::buffer: Set(),
  lateArrivalTokenBackstop::fastPath::ckpt: 0,
  lateArrivalTokenBackstop::fastPath::claimAt: Map(1 -> 0, 2 -> 0),
  lateArrivalTokenBackstop::fastPath::claims: Map(1 -> 0, 2 -> 0),
  lateArrivalTokenBackstop::fastPath::clock: 7,
  lateArrivalTokenBackstop::fastPath::commitAt: Map(1 -> 3, 2 -> 5),
  lateArrivalTokenBackstop::fastPath::committed: [1, 2],
  lateArrivalTokenBackstop::fastPath::events:
    Set("impCrash", "impInserted", "impPartial", "impSkipped"),
  lateArrivalTokenBackstop::fastPath::exps:
    Map(
      1 -> { attempts: 0, phase: "gaveup" },
      2 -> { attempts: 0, phase: "gaveup" }
    ),
  lateArrivalTokenBackstop::fastPath::imp: { b: 0, phase: "idle" },
  lateArrivalTokenBackstop::fastPath::impChecks: 6,
  lateArrivalTokenBackstop::fastPath::impInserts: 3,
  lateArrivalTokenBackstop::fastPath::ledger: Set(),
  lateArrivalTokenBackstop::fastPath::markers: Set(),
  lateArrivalTokenBackstop::fastPath::net: Set(),
  lateArrivalTokenBackstop::fastPath::target:
    Map((1, 1) -> 1, (1, 2) -> 1, (2, 1) -> 0, (2, 2) -> 0),
  lateArrivalTokenBackstop::fastPath::window: [(1, 1), (1, 2)],
  lateArrivalTokenBackstop::fastPath::written: Set(1, 2),
  lateArrivalTokenBackstop::fastPath::writtenAt: Map(1 -> 2, 2 -> 4)
}

[State 24]
{
  lateArrivalTokenBackstop::fastPath::abandoned: Set(),
  lateArrivalTokenBackstop::fastPath::buffer: Set(),
  lateArrivalTokenBackstop::fastPath::ckpt: 0,
  lateArrivalTokenBackstop::fastPath::claimAt: Map(1 -> 0, 2 -> 0),
  lateArrivalTokenBackstop::fastPath::claims: Map(1 -> 0, 2 -> 0),
  lateArrivalTokenBackstop::fastPath::clock: 7,
  lateArrivalTokenBackstop::fastPath::commitAt: Map(1 -> 3, 2 -> 5),
  lateArrivalTokenBackstop::fastPath::committed: [1, 2],
  lateArrivalTokenBackstop::fastPath::events:
    Set("impCrash", "impInserted", "impPartial", "impSkipped"),
  lateArrivalTokenBackstop::fastPath::exps:
    Map(
      1 -> { attempts: 0, phase: "gaveup" },
      2 -> { attempts: 0, phase: "gaveup" }
    ),
  lateArrivalTokenBackstop::fastPath::imp: { b: 1, phase: "done" },
  lateArrivalTokenBackstop::fastPath::impChecks: 7,
  lateArrivalTokenBackstop::fastPath::impInserts: 3,
  lateArrivalTokenBackstop::fastPath::ledger: Set(),
  lateArrivalTokenBackstop::fastPath::markers: Set(),
  lateArrivalTokenBackstop::fastPath::net: Set(),
  lateArrivalTokenBackstop::fastPath::target:
    Map((1, 1) -> 1, (1, 2) -> 1, (2, 1) -> 0, (2, 2) -> 0),
  lateArrivalTokenBackstop::fastPath::window: [(1, 1), (1, 2)],
  lateArrivalTokenBackstop::fastPath::written: Set(1, 2),
  lateArrivalTokenBackstop::fastPath::writtenAt: Map(1 -> 2, 2 -> 4)
}

[State 25]
{
  lateArrivalTokenBackstop::fastPath::abandoned: Set(),
  lateArrivalTokenBackstop::fastPath::buffer: Set(),
  lateArrivalTokenBackstop::fastPath::ckpt: 0,
  lateArrivalTokenBackstop::fastPath::claimAt: Map(1 -> 0, 2 -> 0),
  lateArrivalTokenBackstop::fastPath::claims: Map(1 -> 0, 2 -> 0),
  lateArrivalTokenBackstop::fastPath::clock: 7,
  lateArrivalTokenBackstop::fastPath::commitAt: Map(1 -> 3, 2 -> 5),
  lateArrivalTokenBackstop::fastPath::committed: [1, 2],
  lateArrivalTokenBackstop::fastPath::events:
    Set("impCrash", "impInserted", "impPartial", "impSkipped"),
  lateArrivalTokenBackstop::fastPath::exps:
    Map(
      1 -> { attempts: 0, phase: "gaveup" },
      2 -> { attempts: 0, phase: "gaveup" }
    ),
  lateArrivalTokenBackstop::fastPath::imp: { b: 0, phase: "idle" },
  lateArrivalTokenBackstop::fastPath::impChecks: 7,
  lateArrivalTokenBackstop::fastPath::impInserts: 3,
  lateArrivalTokenBackstop::fastPath::ledger: Set(),
  lateArrivalTokenBackstop::fastPath::markers: Set(),
  lateArrivalTokenBackstop::fastPath::net: Set(),
  lateArrivalTokenBackstop::fastPath::target:
    Map((1, 1) -> 1, (1, 2) -> 1, (2, 1) -> 0, (2, 2) -> 0),
  lateArrivalTokenBackstop::fastPath::window: [(1, 1), (1, 2)],
  lateArrivalTokenBackstop::fastPath::written: Set(1, 2),
  lateArrivalTokenBackstop::fastPath::writtenAt: Map(1 -> 2, 2 -> 4)
}

[State 26]
{
  lateArrivalTokenBackstop::fastPath::abandoned: Set(),
  lateArrivalTokenBackstop::fastPath::buffer: Set(),
  lateArrivalTokenBackstop::fastPath::ckpt: 0,
  lateArrivalTokenBackstop::fastPath::claimAt: Map(1 -> 0, 2 -> 0),
  lateArrivalTokenBackstop::fastPath::claims: Map(1 -> 0, 2 -> 0),
  lateArrivalTokenBackstop::fastPath::clock: 7,
  lateArrivalTokenBackstop::fastPath::commitAt: Map(1 -> 3, 2 -> 5),
  lateArrivalTokenBackstop::fastPath::committed: [1, 2],
  lateArrivalTokenBackstop::fastPath::events:
    Set("impCrash", "impInserted", "impPartial", "impSkipped"),
  lateArrivalTokenBackstop::fastPath::exps:
    Map(
      1 -> { attempts: 0, phase: "gaveup" },
      2 -> { attempts: 0, phase: "gaveup" }
    ),
  lateArrivalTokenBackstop::fastPath::imp: { b: 1, phase: "done" },
  lateArrivalTokenBackstop::fastPath::impChecks: 8,
  lateArrivalTokenBackstop::fastPath::impInserts: 3,
  lateArrivalTokenBackstop::fastPath::ledger: Set(),
  lateArrivalTokenBackstop::fastPath::markers: Set(),
  lateArrivalTokenBackstop::fastPath::net: Set(),
  lateArrivalTokenBackstop::fastPath::target:
    Map((1, 1) -> 1, (1, 2) -> 1, (2, 1) -> 0, (2, 2) -> 0),
  lateArrivalTokenBackstop::fastPath::window: [(1, 1), (1, 2)],
  lateArrivalTokenBackstop::fastPath::written: Set(1, 2),
  lateArrivalTokenBackstop::fastPath::writtenAt: Map(1 -> 2, 2 -> 4)
}

[State 27]
{
  lateArrivalTokenBackstop::fastPath::abandoned: Set(),
  lateArrivalTokenBackstop::fastPath::buffer: Set(),
  lateArrivalTokenBackstop::fastPath::ckpt: 0,
  lateArrivalTokenBackstop::fastPath::claimAt: Map(1 -> 0, 2 -> 0),
  lateArrivalTokenBackstop::fastPath::claims: Map(1 -> 0, 2 -> 0),
  lateArrivalTokenBackstop::fastPath::clock: 7,
  lateArrivalTokenBackstop::fastPath::commitAt: Map(1 -> 3, 2 -> 5),
  lateArrivalTokenBackstop::fastPath::committed: [1, 2],
  lateArrivalTokenBackstop::fastPath::events:
    Set("impCrash", "impInserted", "impPartial", "impSkipped"),
  lateArrivalTokenBackstop::fastPath::exps:
    Map(
      1 -> { attempts: 0, phase: "gaveup" },
      2 -> { attempts: 0, phase: "gaveup" }
    ),
  lateArrivalTokenBackstop::fastPath::imp: { b: 0, phase: "idle" },
  lateArrivalTokenBackstop::fastPath::impChecks: 8,
  lateArrivalTokenBackstop::fastPath::impInserts: 3,
  lateArrivalTokenBackstop::fastPath::ledger: Set(),
  lateArrivalTokenBackstop::fastPath::markers: Set(),
  lateArrivalTokenBackstop::fastPath::net: Set(),
  lateArrivalTokenBackstop::fastPath::target:
    Map((1, 1) -> 1, (1, 2) -> 1, (2, 1) -> 0, (2, 2) -> 0),
  lateArrivalTokenBackstop::fastPath::window: [(1, 1), (1, 2)],
  lateArrivalTokenBackstop::fastPath::written: Set(1, 2),
  lateArrivalTokenBackstop::fastPath::writtenAt: Map(1 -> 2, 2 -> 4)
}

[State 28]
{
  lateArrivalTokenBackstop::fastPath::abandoned: Set(),
  lateArrivalTokenBackstop::fastPath::buffer: Set(),
  lateArrivalTokenBackstop::fastPath::ckpt: 0,
  lateArrivalTokenBackstop::fastPath::claimAt: Map(1 -> 0, 2 -> 0),
  lateArrivalTokenBackstop::fastPath::claims: Map(1 -> 0, 2 -> 0),
  lateArrivalTokenBackstop::fastPath::clock: 7,
  lateArrivalTokenBackstop::fastPath::commitAt: Map(1 -> 3, 2 -> 5),
  lateArrivalTokenBackstop::fastPath::committed: [1, 2],
  lateArrivalTokenBackstop::fastPath::events:
    Set("impCrash", "impInserted", "impPartial", "impSkipped"),
  lateArrivalTokenBackstop::fastPath::exps:
    Map(
      1 -> { attempts: 0, phase: "gaveup" },
      2 -> { attempts: 0, phase: "gaveup" }
    ),
  lateArrivalTokenBackstop::fastPath::imp: { b: 1, phase: "done" },
  lateArrivalTokenBackstop::fastPath::impChecks: 9,
  lateArrivalTokenBackstop::fastPath::impInserts: 3,
  lateArrivalTokenBackstop::fastPath::ledger: Set(),
  lateArrivalTokenBackstop::fastPath::markers: Set(),
  lateArrivalTokenBackstop::fastPath::net: Set(),
  lateArrivalTokenBackstop::fastPath::target:
    Map((1, 1) -> 1, (1, 2) -> 1, (2, 1) -> 0, (2, 2) -> 0),
  lateArrivalTokenBackstop::fastPath::window: [(1, 1), (1, 2)],
  lateArrivalTokenBackstop::fastPath::written: Set(1, 2),
  lateArrivalTokenBackstop::fastPath::writtenAt: Map(1 -> 2, 2 -> 4)
}

[State 29]
{
  lateArrivalTokenBackstop::fastPath::abandoned: Set(),
  lateArrivalTokenBackstop::fastPath::buffer: Set(),
  lateArrivalTokenBackstop::fastPath::ckpt: 0,
  lateArrivalTokenBackstop::fastPath::claimAt: Map(1 -> 0, 2 -> 0),
  lateArrivalTokenBackstop::fastPath::claims: Map(1 -> 0, 2 -> 0),
  lateArrivalTokenBackstop::fastPath::clock: 7,
  lateArrivalTokenBackstop::fastPath::commitAt: Map(1 -> 3, 2 -> 5),
  lateArrivalTokenBackstop::fastPath::committed: [1, 2],
  lateArrivalTokenBackstop::fastPath::events:
    Set("impCrash", "impInserted", "impPartial", "impSkipped"),
  lateArrivalTokenBackstop::fastPath::exps:
    Map(
      1 -> { attempts: 0, phase: "gaveup" },
      2 -> { attempts: 0, phase: "gaveup" }
    ),
  lateArrivalTokenBackstop::fastPath::imp: { b: 0, phase: "idle" },
  lateArrivalTokenBackstop::fastPath::impChecks: 9,
  lateArrivalTokenBackstop::fastPath::impInserts: 3,
  lateArrivalTokenBackstop::fastPath::ledger: Set(),
  lateArrivalTokenBackstop::fastPath::markers: Set(),
  lateArrivalTokenBackstop::fastPath::net: Set(),
  lateArrivalTokenBackstop::fastPath::target:
    Map((1, 1) -> 1, (1, 2) -> 1, (2, 1) -> 0, (2, 2) -> 0),
  lateArrivalTokenBackstop::fastPath::window: [(1, 1), (1, 2)],
  lateArrivalTokenBackstop::fastPath::written: Set(1, 2),
  lateArrivalTokenBackstop::fastPath::writtenAt: Map(1 -> 2, 2 -> 4)
}

[State 30]
{
  lateArrivalTokenBackstop::fastPath::abandoned: Set(),
  lateArrivalTokenBackstop::fastPath::buffer: Set(),
  lateArrivalTokenBackstop::fastPath::ckpt: 0,
  lateArrivalTokenBackstop::fastPath::claimAt: Map(1 -> 0, 2 -> 0),
  lateArrivalTokenBackstop::fastPath::claims: Map(1 -> 0, 2 -> 0),
  lateArrivalTokenBackstop::fastPath::clock: 7,
  lateArrivalTokenBackstop::fastPath::commitAt: Map(1 -> 3, 2 -> 5),
  lateArrivalTokenBackstop::fastPath::committed: [1, 2],
  lateArrivalTokenBackstop::fastPath::events:
    Set("impCrash", "impInserted", "impPartial", "impSkipped"),
  lateArrivalTokenBackstop::fastPath::exps:
    Map(
      1 -> { attempts: 0, phase: "gaveup" },
      2 -> { attempts: 0, phase: "gaveup" }
    ),
  lateArrivalTokenBackstop::fastPath::imp: { b: 1, phase: "done" },
  lateArrivalTokenBackstop::fastPath::impChecks: 10,
  lateArrivalTokenBackstop::fastPath::impInserts: 3,
  lateArrivalTokenBackstop::fastPath::ledger: Set(),
  lateArrivalTokenBackstop::fastPath::markers: Set(),
  lateArrivalTokenBackstop::fastPath::net: Set(),
  lateArrivalTokenBackstop::fastPath::target:
    Map((1, 1) -> 1, (1, 2) -> 1, (2, 1) -> 0, (2, 2) -> 0),
  lateArrivalTokenBackstop::fastPath::window: [(1, 1), (1, 2)],
  lateArrivalTokenBackstop::fastPath::written: Set(1, 2),
  lateArrivalTokenBackstop::fastPath::writtenAt: Map(1 -> 2, 2 -> 4)
}

[State 31]
{
  lateArrivalTokenBackstop::fastPath::abandoned: Set(),
  lateArrivalTokenBackstop::fastPath::buffer: Set(),
  lateArrivalTokenBackstop::fastPath::ckpt: 0,
  lateArrivalTokenBackstop::fastPath::claimAt: Map(1 -> 0, 2 -> 0),
  lateArrivalTokenBackstop::fastPath::claims: Map(1 -> 0, 2 -> 0),
  lateArrivalTokenBackstop::fastPath::clock: 7,
  lateArrivalTokenBackstop::fastPath::commitAt: Map(1 -> 3, 2 -> 5),
  lateArrivalTokenBackstop::fastPath::committed: [1, 2],
  lateArrivalTokenBackstop::fastPath::events:
    Set("impCrash", "impInserted", "impPartial", "impSkipped"),
  lateArrivalTokenBackstop::fastPath::exps:
    Map(
      1 -> { attempts: 0, phase: "gaveup" },
      2 -> { attempts: 0, phase: "gaveup" }
    ),
  lateArrivalTokenBackstop::fastPath::imp: { b: 0, phase: "idle" },
  lateArrivalTokenBackstop::fastPath::impChecks: 10,
  lateArrivalTokenBackstop::fastPath::impInserts: 3,
  lateArrivalTokenBackstop::fastPath::ledger: Set(),
  lateArrivalTokenBackstop::fastPath::markers: Set(),
  lateArrivalTokenBackstop::fastPath::net: Set(),
  lateArrivalTokenBackstop::fastPath::target:
    Map((1, 1) -> 1, (1, 2) -> 1, (2, 1) -> 0, (2, 2) -> 0),
  lateArrivalTokenBackstop::fastPath::window: [(1, 1), (1, 2)],
  lateArrivalTokenBackstop::fastPath::written: Set(1, 2),
  lateArrivalTokenBackstop::fastPath::writtenAt: Map(1 -> 2, 2 -> 4)
}

[State 32]
{
  lateArrivalTokenBackstop::fastPath::abandoned: Set(),
  lateArrivalTokenBackstop::fastPath::buffer: Set(),
  lateArrivalTokenBackstop::fastPath::ckpt: 0,
  lateArrivalTokenBackstop::fastPath::claimAt: Map(1 -> 0, 2 -> 0),
  lateArrivalTokenBackstop::fastPath::claims: Map(1 -> 0, 2 -> 0),
  lateArrivalTokenBackstop::fastPath::clock: 7,
  lateArrivalTokenBackstop::fastPath::commitAt: Map(1 -> 3, 2 -> 5),
  lateArrivalTokenBackstop::fastPath::committed: [1, 2],
  lateArrivalTokenBackstop::fastPath::events:
    Set("impCrash", "impInserted", "impPartial", "impSkipped"),
  lateArrivalTokenBackstop::fastPath::exps:
    Map(
      1 -> { attempts: 0, phase: "gaveup" },
      2 -> { attempts: 0, phase: "gaveup" }
    ),
  lateArrivalTokenBackstop::fastPath::imp: { b: 1, phase: "done" },
  lateArrivalTokenBackstop::fastPath::impChecks: 11,
  lateArrivalTokenBackstop::fastPath::impInserts: 3,
  lateArrivalTokenBackstop::fastPath::ledger: Set(),
  lateArrivalTokenBackstop::fastPath::markers: Set(),
  lateArrivalTokenBackstop::fastPath::net: Set(),
  lateArrivalTokenBackstop::fastPath::target:
    Map((1, 1) -> 1, (1, 2) -> 1, (2, 1) -> 0, (2, 2) -> 0),
  lateArrivalTokenBackstop::fastPath::window: [(1, 1), (1, 2)],
  lateArrivalTokenBackstop::fastPath::written: Set(1, 2),
  lateArrivalTokenBackstop::fastPath::writtenAt: Map(1 -> 2, 2 -> 4)
}

[State 33]
{
  lateArrivalTokenBackstop::fastPath::abandoned: Set(),
  lateArrivalTokenBackstop::fastPath::buffer: Set(),
  lateArrivalTokenBackstop::fastPath::ckpt: 0,
  lateArrivalTokenBackstop::fastPath::claimAt: Map(1 -> 0, 2 -> 0),
  lateArrivalTokenBackstop::fastPath::claims: Map(1 -> 0, 2 -> 0),
  lateArrivalTokenBackstop::fastPath::clock: 7,
  lateArrivalTokenBackstop::fastPath::commitAt: Map(1 -> 3, 2 -> 5),
  lateArrivalTokenBackstop::fastPath::committed: [1, 2],
  lateArrivalTokenBackstop::fastPath::events:
    Set("impCrash", "impInserted", "impPartial", "impSkipped"),
  lateArrivalTokenBackstop::fastPath::exps:
    Map(
      1 -> { attempts: 0, phase: "gaveup" },
      2 -> { attempts: 0, phase: "gaveup" }
    ),
  lateArrivalTokenBackstop::fastPath::imp: { b: 0, phase: "idle" },
  lateArrivalTokenBackstop::fastPath::impChecks: 11,
  lateArrivalTokenBackstop::fastPath::impInserts: 3,
  lateArrivalTokenBackstop::fastPath::ledger: Set(),
  lateArrivalTokenBackstop::fastPath::markers: Set(),
  lateArrivalTokenBackstop::fastPath::net: Set(),
  lateArrivalTokenBackstop::fastPath::target:
    Map((1, 1) -> 1, (1, 2) -> 1, (2, 1) -> 0, (2, 2) -> 0),
  lateArrivalTokenBackstop::fastPath::window: [(1, 1), (1, 2)],
  lateArrivalTokenBackstop::fastPath::written: Set(1, 2),
  lateArrivalTokenBackstop::fastPath::writtenAt: Map(1 -> 2, 2 -> 4)
}

[State 34]
{
  lateArrivalTokenBackstop::fastPath::abandoned: Set(),
  lateArrivalTokenBackstop::fastPath::buffer: Set(),
  lateArrivalTokenBackstop::fastPath::ckpt: 0,
  lateArrivalTokenBackstop::fastPath::claimAt: Map(1 -> 0, 2 -> 0),
  lateArrivalTokenBackstop::fastPath::claims: Map(1 -> 0, 2 -> 0),
  lateArrivalTokenBackstop::fastPath::clock: 7,
  lateArrivalTokenBackstop::fastPath::commitAt: Map(1 -> 3, 2 -> 5),
  lateArrivalTokenBackstop::fastPath::committed: [1, 2],
  lateArrivalTokenBackstop::fastPath::events:
    Set("impCrash", "impInserted", "impPartial", "impSkipped"),
  lateArrivalTokenBackstop::fastPath::exps:
    Map(
      1 -> { attempts: 0, phase: "gaveup" },
      2 -> { attempts: 0, phase: "gaveup" }
    ),
  lateArrivalTokenBackstop::fastPath::imp: { b: 1, phase: "done" },
  lateArrivalTokenBackstop::fastPath::impChecks: 12,
  lateArrivalTokenBackstop::fastPath::impInserts: 3,
  lateArrivalTokenBackstop::fastPath::ledger: Set(),
  lateArrivalTokenBackstop::fastPath::markers: Set(),
  lateArrivalTokenBackstop::fastPath::net: Set(),
  lateArrivalTokenBackstop::fastPath::target:
    Map((1, 1) -> 1, (1, 2) -> 1, (2, 1) -> 0, (2, 2) -> 0),
  lateArrivalTokenBackstop::fastPath::window: [(1, 1), (1, 2)],
  lateArrivalTokenBackstop::fastPath::written: Set(1, 2),
  lateArrivalTokenBackstop::fastPath::writtenAt: Map(1 -> 2, 2 -> 4)
}

[State 35]
{
  lateArrivalTokenBackstop::fastPath::abandoned: Set(),
  lateArrivalTokenBackstop::fastPath::buffer: Set(),
  lateArrivalTokenBackstop::fastPath::ckpt: 0,
  lateArrivalTokenBackstop::fastPath::claimAt: Map(1 -> 0, 2 -> 0),
  lateArrivalTokenBackstop::fastPath::claims: Map(1 -> 0, 2 -> 0),
  lateArrivalTokenBackstop::fastPath::clock: 7,
  lateArrivalTokenBackstop::fastPath::commitAt: Map(1 -> 3, 2 -> 5),
  lateArrivalTokenBackstop::fastPath::committed: [1, 2],
  lateArrivalTokenBackstop::fastPath::events:
    Set("impCrash", "impInserted", "impPartial", "impSkipped"),
  lateArrivalTokenBackstop::fastPath::exps:
    Map(
      1 -> { attempts: 0, phase: "gaveup" },
      2 -> { attempts: 0, phase: "gaveup" }
    ),
  lateArrivalTokenBackstop::fastPath::imp: { b: 0, phase: "idle" },
  lateArrivalTokenBackstop::fastPath::impChecks: 12,
  lateArrivalTokenBackstop::fastPath::impInserts: 3,
  lateArrivalTokenBackstop::fastPath::ledger: Set(),
  lateArrivalTokenBackstop::fastPath::markers: Set(),
  lateArrivalTokenBackstop::fastPath::net: Set(),
  lateArrivalTokenBackstop::fastPath::target:
    Map((1, 1) -> 1, (1, 2) -> 1, (2, 1) -> 0, (2, 2) -> 0),
  lateArrivalTokenBackstop::fastPath::window: [(1, 1), (1, 2)],
  lateArrivalTokenBackstop::fastPath::written: Set(1, 2),
  lateArrivalTokenBackstop::fastPath::writtenAt: Map(1 -> 2, 2 -> 4)
}

[State 36]
{
  lateArrivalTokenBackstop::fastPath::abandoned: Set(),
  lateArrivalTokenBackstop::fastPath::buffer: Set(),
  lateArrivalTokenBackstop::fastPath::ckpt: 0,
  lateArrivalTokenBackstop::fastPath::claimAt: Map(1 -> 0, 2 -> 0),
  lateArrivalTokenBackstop::fastPath::claims: Map(1 -> 0, 2 -> 0),
  lateArrivalTokenBackstop::fastPath::clock: 7,
  lateArrivalTokenBackstop::fastPath::commitAt: Map(1 -> 3, 2 -> 5),
  lateArrivalTokenBackstop::fastPath::committed: [1, 2],
  lateArrivalTokenBackstop::fastPath::events:
    Set("impCrash", "impInserted", "impPartial", "impSkipped"),
  lateArrivalTokenBackstop::fastPath::exps:
    Map(
      1 -> { attempts: 0, phase: "gaveup" },
      2 -> { attempts: 0, phase: "gaveup" }
    ),
  lateArrivalTokenBackstop::fastPath::imp: { b: 1, phase: "done" },
  lateArrivalTokenBackstop::fastPath::impChecks: 13,
  lateArrivalTokenBackstop::fastPath::impInserts: 3,
  lateArrivalTokenBackstop::fastPath::ledger: Set(),
  lateArrivalTokenBackstop::fastPath::markers: Set(),
  lateArrivalTokenBackstop::fastPath::net: Set(),
  lateArrivalTokenBackstop::fastPath::target:
    Map((1, 1) -> 1, (1, 2) -> 1, (2, 1) -> 0, (2, 2) -> 0),
  lateArrivalTokenBackstop::fastPath::window: [(1, 1), (1, 2)],
  lateArrivalTokenBackstop::fastPath::written: Set(1, 2),
  lateArrivalTokenBackstop::fastPath::writtenAt: Map(1 -> 2, 2 -> 4)
}

[State 37]
{
  lateArrivalTokenBackstop::fastPath::abandoned: Set(),
  lateArrivalTokenBackstop::fastPath::buffer: Set(),
  lateArrivalTokenBackstop::fastPath::ckpt: 0,
  lateArrivalTokenBackstop::fastPath::claimAt: Map(1 -> 0, 2 -> 0),
  lateArrivalTokenBackstop::fastPath::claims: Map(1 -> 0, 2 -> 0),
  lateArrivalTokenBackstop::fastPath::clock: 7,
  lateArrivalTokenBackstop::fastPath::commitAt: Map(1 -> 3, 2 -> 5),
  lateArrivalTokenBackstop::fastPath::committed: [1, 2],
  lateArrivalTokenBackstop::fastPath::events:
    Set("impCrash", "impInserted", "impPartial", "impSkipped"),
  lateArrivalTokenBackstop::fastPath::exps:
    Map(
      1 -> { attempts: 0, phase: "gaveup" },
      2 -> { attempts: 0, phase: "gaveup" }
    ),
  lateArrivalTokenBackstop::fastPath::imp: { b: 0, phase: "idle" },
  lateArrivalTokenBackstop::fastPath::impChecks: 13,
  lateArrivalTokenBackstop::fastPath::impInserts: 3,
  lateArrivalTokenBackstop::fastPath::ledger: Set(),
  lateArrivalTokenBackstop::fastPath::markers: Set(),
  lateArrivalTokenBackstop::fastPath::net: Set(),
  lateArrivalTokenBackstop::fastPath::target:
    Map((1, 1) -> 1, (1, 2) -> 1, (2, 1) -> 0, (2, 2) -> 0),
  lateArrivalTokenBackstop::fastPath::window: [(1, 1), (1, 2)],
  lateArrivalTokenBackstop::fastPath::written: Set(1, 2),
  lateArrivalTokenBackstop::fastPath::writtenAt: Map(1 -> 2, 2 -> 4)
}

[State 38]
{
  lateArrivalTokenBackstop::fastPath::abandoned: Set(),
  lateArrivalTokenBackstop::fastPath::buffer: Set(),
  lateArrivalTokenBackstop::fastPath::ckpt: 0,
  lateArrivalTokenBackstop::fastPath::claimAt: Map(1 -> 0, 2 -> 0),
  lateArrivalTokenBackstop::fastPath::claims: Map(1 -> 0, 2 -> 0),
  lateArrivalTokenBackstop::fastPath::clock: 7,
  lateArrivalTokenBackstop::fastPath::commitAt: Map(1 -> 3, 2 -> 5),
  lateArrivalTokenBackstop::fastPath::committed: [1, 2],
  lateArrivalTokenBackstop::fastPath::events:
    Set("impCrash", "impInserted", "impPartial", "impSkipped"),
  lateArrivalTokenBackstop::fastPath::exps:
    Map(
      1 -> { attempts: 0, phase: "gaveup" },
      2 -> { attempts: 0, phase: "gaveup" }
    ),
  lateArrivalTokenBackstop::fastPath::imp: { b: 1, phase: "done" },
  lateArrivalTokenBackstop::fastPath::impChecks: 14,
  lateArrivalTokenBackstop::fastPath::impInserts: 3,
  lateArrivalTokenBackstop::fastPath::ledger: Set(),
  lateArrivalTokenBackstop::fastPath::markers: Set(),
  lateArrivalTokenBackstop::fastPath::net: Set(),
  lateArrivalTokenBackstop::fastPath::target:
    Map((1, 1) -> 1, (1, 2) -> 1, (2, 1) -> 0, (2, 2) -> 0),
  lateArrivalTokenBackstop::fastPath::window: [(1, 1), (1, 2)],
  lateArrivalTokenBackstop::fastPath::written: Set(1, 2),
  lateArrivalTokenBackstop::fastPath::writtenAt: Map(1 -> 2, 2 -> 4)
}

[State 39]
{
  lateArrivalTokenBackstop::fastPath::abandoned: Set(),
  lateArrivalTokenBackstop::fastPath::buffer: Set(),
  lateArrivalTokenBackstop::fastPath::ckpt: 1,
  lateArrivalTokenBackstop::fastPath::claimAt: Map(1 -> 0, 2 -> 0),
  lateArrivalTokenBackstop::fastPath::claims: Map(1 -> 0, 2 -> 0),
  lateArrivalTokenBackstop::fastPath::clock: 7,
  lateArrivalTokenBackstop::fastPath::commitAt: Map(1 -> 3, 2 -> 5),
  lateArrivalTokenBackstop::fastPath::committed: [1, 2],
  lateArrivalTokenBackstop::fastPath::events:
    Set("impCrash", "impInserted", "impPartial", "impSkipped"),
  lateArrivalTokenBackstop::fastPath::exps:
    Map(
      1 -> { attempts: 0, phase: "gaveup" },
      2 -> { attempts: 0, phase: "gaveup" }
    ),
  lateArrivalTokenBackstop::fastPath::imp: { b: 0, phase: "idle" },
  lateArrivalTokenBackstop::fastPath::impChecks: 14,
  lateArrivalTokenBackstop::fastPath::impInserts: 3,
  lateArrivalTokenBackstop::fastPath::ledger: Set(),
  lateArrivalTokenBackstop::fastPath::markers: Set(),
  lateArrivalTokenBackstop::fastPath::net: Set(),
  lateArrivalTokenBackstop::fastPath::target:
    Map((1, 1) -> 1, (1, 2) -> 1, (2, 1) -> 0, (2, 2) -> 0),
  lateArrivalTokenBackstop::fastPath::window: [(1, 1), (1, 2)],
  lateArrivalTokenBackstop::fastPath::written: Set(1, 2),
  lateArrivalTokenBackstop::fastPath::writtenAt: Map(1 -> 2, 2 -> 4)
}

[ok] No violation found (19876ms at 75 traces/second).
Trace length statistics: max=40, min=14, average=17.14
You may increase --max-samples and --max-steps.
Use --verbosity to produce more (or less) output.
Use --seed=0xbc8a8 --backend=typescript to reproduce.
